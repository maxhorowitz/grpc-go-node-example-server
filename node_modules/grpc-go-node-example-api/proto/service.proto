syntax = "proto3";

package registry;

option go_package = "proto/";

service Registry {
    rpc GetLast(FirstName) returns (LastName) {}
}

message FirstName {
    string name = 1;
}

message LastName {
    string name = 1;
}